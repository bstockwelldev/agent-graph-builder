---
"@bstockwelldev/agent-graph-sdk": minor
---

Transforms library: `client.transforms` (CRUD, usages and versions for `/api/transforms`), `transformDefinitionSchema`, and `transform_id` on edge transforms (a library reference; `type` is now optional when it's set). `transforms` joins `BindableResourceKind`.
