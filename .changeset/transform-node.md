---
"@bstockwelldev/agent-graph-sdk": minor
---

New `transform` node type (select / wrap / format_message / coerce, or bound to a Transforms library entry via `transformId`): `nodeTypeSchema`, `defaultConfig`, node summaries/labels, and the `transform → transformId → transforms` binding.
