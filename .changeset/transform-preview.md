---
"@bstockwelldev/agent-graph-sdk": minor
---

`client.transforms.preview(transform, value)` (`POST /api/transforms/preview`): applies an inline or library transform to a sample value and returns `{ ok, output, error }` without running a graph.
