{
  "graph": {
    "title": "Agent Graph Builder",
    "aliases": ["graph-builder", "agent-graph"],
    "repo": "agent-graph-builder",
    "runtime": {
      "up": {
        "ps1": "scripts/dev.ps1",
        "sh": "scripts/dev.sh"
      },
      "urls": {
        "app": "http://localhost:3000",
        "api": "http://localhost:8000"
      }
    },
    "checks": {
      "fast": "cd backend && uv run pytest -q",
      "full": "cd backend && uv run pytest"
    }
  }
}
