{
  "name": "agent-graph-builder",
  "version": "0.2.0",
  "private": true,
  "description": "Agent Graph Builder — visual graph authoring studio and TypeScript SDK",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm --filter @bstockwelldev/agent-graph-sdk run build && pnpm --filter @bstockwelldev/agent-graph-studio run build",
    "build:sdk": "pnpm --filter @bstockwelldev/agent-graph-sdk run build",
    "dev": "pnpm --filter @bstockwelldev/agent-graph-studio run dev",
    "test": "pnpm run build:sdk && pnpm --filter @bstockwelldev/agent-graph-sdk run test && pnpm --filter @bstockwelldev/agent-graph-studio run test",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1"
  }
}
